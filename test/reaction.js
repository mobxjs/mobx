var test = require('tape')
var mobx = require('..')
var reaction = mobx.reaction

test('basic')


test('effect is untracked')

test('effect fireImmediately is honored')


test('effect debounce is honored')


test('effect debounce + fireImmediately is honored')